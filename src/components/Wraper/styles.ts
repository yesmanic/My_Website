import styled from "styled-components";

export const WraperComponent = styled.div`
    display: flex;
    width: 100%;
    justify-content: center;

    .container {
        width: min(100%, 50rem);
    }
`